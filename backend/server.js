{/*const express = require('express');
const mysql = require('mysql')
const cors = require('cors')

const app = express()
app.use(cors())

const db = mysql.createConnection({
    host:"localhost",
    user: 'root',
    password:'MySQL@123#',
    database: 'capstone_hris'
})

app.get('/', (re, res)=> {return re.json("From Backend Side");})
app.get('/users', (req, res) =>{})
app.listen(8081, ()=>{ console.log("listenting");})*/}